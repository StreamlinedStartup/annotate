#!/bin/sh
provider="$1"
event="$2"
shift 2 2>/dev/null || true
if command -v sprout >/dev/null 2>&1; then
  exec sprout hook run --provider "$provider" --event "$event"
fi
echo "sprout hook warning: sprout executable not found on PATH" >&2
exit 0
